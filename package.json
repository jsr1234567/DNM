{
  "name": "dnm",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run db:seed:mock && bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "db:setup": "bun scripts/setup-db.ts",
    "db:seed:mock": "bun scripts/seed-mock-mailboxes.ts",
    "db:reindex": "bun scripts/rebuild-indexes.ts",
    "db:reset": "bun scripts/reset-local-data.ts",
    "db:reset:user": "bun scripts/reset-user-data.ts",
    "users:add": "bun scripts/add-local-user.ts",
    "users:approve": "bun scripts/approve-local-user.ts",
    "gmail:import": "bun scripts/import-gmail.ts",
    "memory:generate": "bun scripts/generate-memory.ts",
    "bounties:generate": "bun scripts/generate-bounties.ts",
    "bounties:match": "bun scripts/match-bounties.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "spectrum-ts": "^12.8.0",
    "sqlite-vec": "0.1.8"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/bun": "latest"
  }
}
